<%= simple_form_for(@organization) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit, class: "btn btn-primary"  %>
  </div>
<% end %>
